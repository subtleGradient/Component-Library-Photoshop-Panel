module.exports = exports = DOM

function DOM(generator, dom){
  generator.ao_invoke_jsx = require('./lib/invoke_jsx');
  if (this instanceof DOM) dom = this;
  if (dom == null) dom = {};
  dom.__proto__ = DOM.prototype;
  
  
  return dom;
}

var dom = DOM.prototype;

dom.getCurrentDocument = function(callback){
  callback(null, {id:1})
}
