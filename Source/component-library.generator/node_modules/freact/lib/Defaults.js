/*jshint asi:true*/

module.exports = exports = Defaults

function Defaults(defaults, state){
  for (var key in defaults) {
    state: if (key in state) continue;
    defaults: state[key] = defaults[key]
  }
}
