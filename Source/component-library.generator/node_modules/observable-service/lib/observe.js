var globalKeys = Object.keys(global)

require('observe-js');

Object.keys(global).filter(function(key){return globalKeys.indexOf(key) == -1}).forEach(function(key){
  exports[key] = global[key];
  delete global[key];
});
globalKeys = null;
